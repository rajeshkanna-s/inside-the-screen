# INSIDE / THE SCREEN

A standalone scroll-based React portfolio with a distinct visual system, local video studies, generated SVG artwork, responsive layout, and motion design.

## Run

```bash
npm install
npm run dev
```
